# Miscellaneous
*.class
*.log
*.pyc
*.swp
*.swo
*~
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line is
# commented out by default.
#.vscode/

# Flutter / Dart / Pub
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/

# Symbolication
app.*.symbols

# Obfuscation
app.*.map.json

# Tests / coverage
coverage/
*.lcov
test/**/failures/

# Local env & secrets (track `.env.example` if you use a template)
.env
.env.*
!.env.example

# Local path / git dependency overrides
pubspec_overrides.yaml

# Android Studio / Gradle build outputs (per-variant apk dirs)
android/app/debug
android/app/profile
android/app/release

# FVM — keep `.fvmrc` + `.fvm/fvm_config.json` in git; ignore SDK + cache
.fvm/flutter_sdk
.fvm/versions/

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Merge / backup noise
*.orig
